# Runtime dependencies — source of truth.
# Regenerate the pinned lock after editing:
#   pip-compile requirements.in   (writes requirements.txt)
# Upgrade a single package:
#   pip-compile --upgrade-package <name> requirements.in

# Web / API stack
django>=6.1,<7
djangorestframework>=3.18.0
django-filter>=26.1
dj-database-url>=2.2
psycopg2-binary>=2.9
django-cors-headers>=4.3
drf-spectacular>=0.30.0          # OpenAPI 3 schema + Swagger/Redoc docs
gunicorn>=22.0
sentry-sdk>=2.67.1

# SEC client + HTM/HTML parsing
requests>=2.31
tenacity>=8.2
beautifulsoup4>=4.12
lxml>=5.0          # BeautifulSoup parser backend (src/sec_edgar/parsers/htm.py)

# Async processing (Phase 7) — Celery with a Redis broker/result backend
celery>=5.3
redis>=8.1.0

# Content extraction (Phase 8) — Apache Tika client (talks to a Tika server sidecar)
tika>=3.3.2

# Config
python-dotenv>=1.0
