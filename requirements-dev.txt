# Local dev + CI (install alongside requirements.txt)
pytest>=9.1.1
pytest-django>=4.12.0
pytest-cov>=7.1.0
ruff>=0.15.22
responses>=0.26.2
factory-boy>=3.3.3
freezegun>=1.5.5

# Type checking (advisory) + hooks + audit
mypy>=2.3.0,<2.4  # capped: django-stubs[compatible-mypy] requires mypy<2.2
django-stubs[compatible-mypy]>=6.0.7
pre-commit>=4.6.0
pip-audit>=2.10.1
